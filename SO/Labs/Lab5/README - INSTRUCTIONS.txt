HOW
TO
RUN
----------------------------------------------------------------------
1. Run the makefile:
	$ make

2. Give permission to "test-everything.sh" to run as a program
(Right click >> "Properties" >> "Permissions" >> "Execute" checkbox):

3. Run it:
	$ ./test-everything.sh

4. Outputs will be in folder "out/".