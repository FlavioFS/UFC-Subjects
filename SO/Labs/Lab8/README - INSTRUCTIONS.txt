HOW
TO
RUN
----------------------------------------------------------------------
1. Run the makefile:
	$ make

2. Give the following file permission to run as a program.
Not that makefile will attempt to automate this step.
(Right click >> "Properties" >> "Permissions" >> "Run" checkbox):
	* test-everything.sh

3. Run it
	$ ./test-everything.sh